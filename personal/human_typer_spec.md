# Human-Like Typer — 구현명세서

**프로젝트명:** Human-Like Typer  
**버전:** v1.0  
**작성일:** 2025-02-15  
**최종 수정:** 2025-02-15 (피드백 반영 v1.0.1)  
**환경:** Windows 11 Home / Python 3.x / Visual Studio Code  

---

## 1. 프로젝트 개요

### 1.1 목적

클립보드 또는 직접 입력한 텍스트를, 브라우저 등의 입력 칸에 **실제 사람이 키보드로 타이핑하는 것처럼** OS 레벨 키보드 이벤트로 한 글자씩 입력하는 데스크톱 애플리케이션.

일반적인 `Ctrl+V` 붙여넣기는 브라우저에서 `paste` 이벤트로 감지되지만, 이 프로그램은 `keydown → keypress → input → keyup` 이벤트 시퀀스를 문자 단위로 발생시켜 **수기 입력과 동일하게 인식**되도록 한다.

### 1.2 현실적 제한 사항

> ⚠️ OS 레벨 키 이벤트를 발생시키므로 대부분의 일반 입력 필드에서는 `paste` 이벤트 없이 정상 입력되지만, **일부 사이트/보안 필드(로그인, 결제, CAPTCHA, 봇 방지 구간 등)에서는 synthetic input 감지 또는 자동화 차단**이 적용될 수 있다. 이는 브라우저/웹앱 측 구현에 의존하며 본 프로그램으로 제어할 수 없는 영역이다.

### 1.3 사용 주의사항

> ⚠️ 본 프로그램은 반복 입력 보조, 접근성 지원, 테스트 자동화 등 정당한 용도를 위해 설계되었다. 사용자는 아래 사항을 준수해야 한다:
> - **사용이 허용된 환경에서만 사용**할 것
> - **보안/인증/결제 등 민감한 입력 필드에는 사용을 지양**할 것
> - 대상 사이트/서비스의 이용약관을 확인하고, 자동 입력이 약관 위반에 해당하지 않는지 스스로 판단할 것
>
> 본 프로그램의 사용으로 인한 계정 제재, 서비스 이용 제한 등의 결과는 사용자 본인의 책임이다.

### 1.4 버전 범위

| 항목 | v1.0 범위 | v1.1 예정 |
|------|-----------|-----------|
| 트리거 키 | F1~F12 단일키만 | Ctrl+Shift+조합, Alt+조합 등 |
| 대상 문자 | 영문/숫자/특수문자 (QWERTY) | 한글 지원 |
| 오타 수정 확률 | 고정값 | 단어 길이/문장 위치에 따른 동적 가중 |
| 키 입력 방식 | 간편 모드 + 정교 모드 | - |

### 1.5 대상 문자

QWERTY 자판 기준 입력 가능한 모든 문자:
- 영문 대소문자 (a-z, A-Z)
- 숫자 (0-9)
- 특수문자 (`~!@#$%^&*()-_=+[]{}\|;:'",.<>/?`)
- 공백(Space), 탭(Tab), 개행(Enter)

> 한글은 v1.0 범위에서 제외. 향후 확장 가능.

### 1.6 기술 스택

| 구분 | 기술 | 비고 |
|------|------|------|
| 언어 | Python 3.9+ | |
| GUI | CustomTkinter | 모던 다크/라이트 테마, 토글/슬라이더 위젯 |
| 키보드 시뮬레이션 | pynput | OS 레벨 키 이벤트 발생 + 핫키 리스너 |
| 클립보드 | pyperclip | 클립보드 텍스트 읽기 |
| 시각화 | matplotlib (선택) | 타이밍 시각화용. 없어도 핵심 동작에 영향 없음 |

```
pip install customtkinter pynput pyperclip matplotlib
```

---

## 2. 프로젝트 구조

```
human-typer/
├── main.py                  # 진입점 (GUI 실행)
├── core/
│   ├── __init__.py
│   ├── typer_engine.py      # 타이핑 엔진 (타이밍 + 오타 + 키 입력 시뮬레이션)
│   ├── timing_model.py      # 타이밍 계산 모듈 (딜레이 결정 로직)
│   ├── typo_model.py        # 오타 생성/수정 모듈
│   ├── keyboard_map.py      # QWERTY 인접 키 맵 + Shift 맵
│   ├── clipboard.py         # 클립보드 읽기/모니터링
│   ├── focus_monitor.py     # 활성 창 감지 + 포커스 이탈 시 자동 일시정지
│   └── text_preprocessor.py # 텍스트 전처리 (공백/개행 정규화, 길이 제한)
├── gui/
│   ├── __init__.py
│   ├── app.py               # 메인 윈도우 (레이아웃 조합)
│   ├── input_panel.py       # 입력 소스 영역 (클립보드 미리보기 + 직접 입력)
│   ├── settings_panel.py    # 설정 영역 (타이밍/오타 옵션)
│   ├── control_panel.py     # 컨트롤 영역 (트리거/시작/정지/로그)
│   └── stats_dialog.py      # 통계 요약 + 타이밍 시각화 다이얼로그
├── presets/
│   ├── default.json         # 기본 프리셋
│   ├── fast_accurate.json   # 빠르고 정확한 타이핑
│   ├── slow_natural.json    # 느리고 자연스러운 타이핑
│   └── sloppy_beginner.json # 오타 많은 초보 타이핑
├── config.json              # 사용자 설정 (마지막 상태 자동 저장)
└── requirements.txt
```

---

## 3. 핵심 엔진 (`core/`)

### 3.1 타이밍 모델 (`timing_model.py`)

각 글자를 입력하기 전, 해당 글자에 대한 딜레이(ms)를 계산하는 모듈. 여러 요소를 조합하여 최종 딜레이를 결정한다.

#### 3.1.1 기본 딜레이

| 파라미터 | 타입 | 범위 | 기본값 | 설명 |
|----------|------|------|--------|------|
| `base_delay_ms` | int | 20~500 | 70 | 글자 간 기본 딜레이 (ms) |
| `delay_variance_ms` | int | 0~200 | 30 | 랜덤 편차 범위 (±ms) |

**계산 방식:**
```
raw_delay = base_delay_ms + random.gauss(0, delay_variance_ms / 2)
```
- `uniform` 대신 **가우시안 분포**를 사용하여, 대부분의 딜레이가 base 근처에 모이고 가끔 크게 벗어나는 자연스러운 패턴 생성.
- 최솟값 클램핑: `max(15, raw_delay)` — 15ms 미만은 비현실적.

#### 3.1.2 단어 경계 타이밍 (Word Boundary) `[토글 ON/OFF]`

사람은 단어 내부 글자는 빠르게, 스페이스 후 새 단어 첫 글자는 느리게 입력하는 경향이 있다.

| 파라미터 | 타입 | 범위 | 기본값 | 설명 |
|----------|------|------|--------|------|
| `intra_word_speed_factor` | float | 0.3~1.0 | 0.8 | 단어 내부 글자 딜레이 배율. 0.8 = 20% 빠르게 |
| `inter_word_pause_ms` | int | 0~500 | 120 | 스페이스 다음 글자까지 추가 딜레이 (ms) |

**적용 로직:**
```python
if 이전_글자 == ' ':
    # 새 단어 시작 → 느리게
    delay += inter_word_pause_ms + random.gauss(0, inter_word_pause_ms * 0.2)
elif 이전_글자 != ' ' and 현재_글자 != ' ':
    # 단어 내부 → 빠르게
    delay *= intra_word_speed_factor
```

#### 3.1.3 구두점 일시정지 (Punctuation Pause) `[토글 ON/OFF]`

마침표, 쉼표, 물음표 등 뒤에 사람은 잠시 멈추는 경향이 있다 (다음 문장을 생각하는 시간).

| 파라미터 | 타입 | 범위 | 기본값 | 설명 |
|----------|------|------|--------|------|
| `punctuation_pause_ms` | int | 0~1000 | 200 | 구두점 뒤 추가 딜레이 (ms) |

**적용 조건:** 이전 글자가 `.` `,` `!` `?` `:` `;` 중 하나일 때.
```python
if 이전_글자 in '.,!?:;':
    delay += punctuation_pause_ms + random.gauss(0, punctuation_pause_ms * 0.3)
```

#### 3.1.4 Shift 키 패널티 (Shift Penalty) `[토글 ON/OFF]`

대문자나 `!@#$%^&*()` 등 Shift 조합이 필요한 문자는 물리적으로 두 키를 동시에 누르므로 미세하게 느리다.

| 파라미터 | 타입 | 범위 | 기본값 | 설명 |
|----------|------|------|--------|------|
| `shift_penalty_ms` | int | 0~100 | 25 | Shift 조합 문자 입력 시 추가 딜레이 (ms) |

**적용 조건:** 현재 글자가 대문자이거나 Shift 조합 특수문자일 때.
```python
SHIFT_CHARS = set('ABCDEFGHIJKLMNOPQRSTUVWXYZ~!@#$%^&*()_+{}|:"<>?')
if 현재_글자 in SHIFT_CHARS:
    delay += shift_penalty_ms
```

#### 3.1.5 동일 글자 연속 가속 (Double Letter Speed) `[토글 ON/OFF]`

같은 글자를 연속 입력할 때 (예: "ll", "ss", "ee") 사람은 같은 손가락을 빠르게 반복하므로 더 빠르다.

| 파라미터 | 타입 | 범위 | 기본값 | 설명 |
|----------|------|------|--------|------|
| `double_letter_speed_factor` | float | 0.3~1.0 | 0.6 | 동일 글자 연속 시 딜레이 배율. 0.6 = 40% 빠르게 |

**적용 조건:**
```python
if 현재_글자.lower() == 이전_글자.lower():
    delay *= double_letter_speed_factor
```

#### 3.1.6 버스트 타이핑 (Burst Typing) `[토글 ON/OFF]`

사람은 2~5글자를 빠르게 연타한 뒤 미세하게 쉬는 패턴이 있다. 일정한 속도로 타이핑하지 않는다.

| 파라미터 | 타입 | 범위 | 기본값 | 설명 |
|----------|------|------|--------|------|
| `burst_length_min` | int | 2~10 | 2 | 버스트 최소 길이 (글자 수) |
| `burst_length_max` | int | 2~10 | 5 | 버스트 최대 길이 (글자 수) |
| `burst_pause_ms` | int | 0~200 | 40 | 버스트 종료 후 micro-pause (ms) |

**동작 방식:**
```python
burst_size = random.randint(burst_length_min, burst_length_max)
burst_counter = 0

for char in text:
    burst_counter += 1
    if burst_counter >= burst_size:
        delay += burst_pause_ms + random.gauss(0, burst_pause_ms * 0.3)
        burst_counter = 0
        burst_size = random.randint(burst_length_min, burst_length_max)  # 다음 버스트 길이 재설정
```

#### 3.1.7 개행/문단 경계 일시정지 (Newline Pause) `[토글 ON/OFF]`

Enter(개행) 직후는 마침표보다 더 긴 pause가 발생한다. 새 문단/줄을 시작할 때 다음 내용을 생각하는 시간.

| 파라미터 | 타입 | 범위 | 기본값 | 설명 |
|----------|------|------|--------|------|
| `newline_pause_ms` | int | 0~2000 | 400 | 개행 뒤 추가 딜레이 (ms) |

**적용 조건:** 이전 글자가 `\n`일 때.
```python
if prev_char == '\n':
    delay += newline_pause_ms + random.gauss(0, newline_pause_ms * 0.3)
```

> 이 옵션이 ON이면, word boundary의 inter_word_pause보다 우선 적용된다 (개행 뒤 첫 글자는 newline_pause만 적용, word boundary pause는 중복 적용하지 않음).

#### 3.1.8 타이핑 피로 (Fatigue) `[토글 ON/OFF]`

긴 텍스트를 입력할수록 뒤로 갈수록 미세하게 느려지는 효과.

| 파라미터 | 타입 | 범위 | 기본값 | 설명 |
|----------|------|------|--------|------|
| `fatigue_factor` | float | 0.0~0.5 | 0.05 | 피로 증가율. 0.05 = 텍스트 끝에서 5% 느려짐 |

**적용 방식:**
```python
progress = current_index / total_length  # 0.0 ~ 1.0
fatigue_multiplier = 1.0 + (fatigue_factor * progress)
delay *= fatigue_multiplier
```
- 텍스트 시작: `fatigue_multiplier = 1.0` (영향 없음)
- 텍스트 절반: `fatigue_multiplier = 1.025` (2.5% 느림)
- 텍스트 끝: `fatigue_multiplier = 1.05` (5% 느림)

#### 3.1.9 최종 딜레이 계산 파이프라인

모든 요소를 순서대로 적용하여 최종 딜레이를 결정한다. 각 요소의 기여분을 **구조화된 딜레이 분해 기록**으로 남겨 통계/시각화/디버깅에 활용한다.

```python
def calculate_delay(self, char, prev_char, index, total_length) -> tuple[float, dict]:
    breakdown = {}  # 딜레이 요소 분해 기록

    # 1. 기본 딜레이 (가우시안)
    delay = self.base_delay_ms + random.gauss(0, self.delay_variance_ms / 2)
    breakdown['base'] = delay

    # 2. 개행/문단 경계 (ON일 때만, word boundary보다 우선)
    if self.newline_pause_enabled and prev_char == '\n':
        add = self.newline_pause_ms * (1 + random.gauss(0, 0.3))
        delay += add
        breakdown['newline'] = add
    # 3. 단어 경계 (ON일 때만, 개행이 아닐 때)
    elif self.word_boundary_enabled:
        if prev_char == ' ':
            add = self.inter_word_pause_ms * (1 + random.gauss(0, 0.2))
            delay += add
            breakdown['inter_word'] = add
        elif prev_char and prev_char != ' ' and char != ' ':
            factor = self.intra_word_speed_factor
            delay *= factor
            breakdown['intra_word_factor'] = factor

    # 4. 구두점 일시정지 (ON일 때만)
    if self.punctuation_pause_enabled and prev_char in '.,!?:;':
        add = self.punctuation_pause_ms * (1 + random.gauss(0, 0.3))
        delay += add
        breakdown['punctuation'] = add

    # 5. Shift 패널티 (ON일 때만)
    if self.shift_penalty_enabled and char in SHIFT_CHARS:
        delay += self.shift_penalty_ms
        breakdown['shift'] = self.shift_penalty_ms

    # 6. 동일 글자 연속 가속 (ON일 때만)
    if self.double_letter_enabled and prev_char and char.lower() == prev_char.lower():
        factor = self.double_letter_speed_factor
        delay *= factor
        breakdown['double_letter_factor'] = factor

    # 7. 버스트 타이핑 micro-pause (ON일 때만, 버스트 경계에서)
    if self.burst_enabled and self._is_burst_boundary():
        add = self.burst_pause_ms * (1 + random.gauss(0, 0.3))
        delay += add
        breakdown['burst'] = add

    # 8. 피로 (ON일 때만)
    if self.fatigue_enabled:
        progress = index / max(total_length, 1)
        multiplier = 1.0 + self.fatigue_factor * progress
        delay *= multiplier
        breakdown['fatigue_multiplier'] = multiplier

    # 9. 최종 클램핑
    final = max(15, delay)
    breakdown['final'] = final
    return final, breakdown
```

**딜레이 분해 기록 (`breakdown`) 활용:**
- 로그 출력 시: `[00:01.841] 'q' 입력 (143ms) [base:68 + inter_word:+120 × fatigue:×1.02]`
- 통계 다이얼로그에서: 각 요소별 평균 기여분 표시
- 타이밍 시각화에서: 색상/마커로 어떤 규칙이 적용되었는지 구분

---

### 3.2 오타 모델 (`typo_model.py`)

#### 3.2.1 오타 발생 확률

| 파라미터 | 타입 | 범위 | 기본값 | 설명 |
|----------|------|------|--------|------|
| `typo_prob` | int | 0~9999 | 30 | 오타 발생 확률. 만분율. 30 = 0.30% |
| `typo_revision_prob` | int | 0~99 | 85 | 오타 발생 시 수정할 확률. 백분율. 85 = 85% |

**확률 변환:**
```python
actual_typo_prob = typo_prob / 10000   # 30 → 0.003 (0.3%)
actual_revision_prob = typo_revision_prob / 100  # 85 → 0.85 (85%)
```

#### 3.2.2 오타 유형

각 오타 유형은 독립적으로 토글 ON/OFF 가능. 활성화된 유형 중 랜덤으로 선택.

| 오타 유형 | 토글 | 기본 | 설명 | 예시 |
|-----------|------|------|------|------|
| **인접 키 (Adjacent Key)** | `[ON/OFF]` | ON | QWERTY 레이아웃에서 물리적으로 인접한 키를 대신 입력 | `e` → `w` 또는 `r` |
| **글자 전치 (Transposition)** | `[ON/OFF]` | OFF | 연속 두 글자의 순서가 뒤바뀜 | `the` → `teh` |
| **이중 입력 (Double Strike)** | `[ON/OFF]` | OFF | 같은 키를 실수로 두 번 입력 | `test` → `tesst` |

#### 3.2.3 인접 키 맵 (`keyboard_map.py`)

QWERTY 물리 배열 기준으로 각 키의 인접 키를 매핑:

```python
ADJACENT_KEYS = {
    # 숫자 행
    '1': ['2', 'q'],
    '2': ['1', '3', 'q', 'w'],
    '3': ['2', '4', 'w', 'e'],
    # ...
    # 상단 행
    'q': ['1', '2', 'w', 'a'],
    'w': ['2', '3', 'q', 'e', 'a', 's'],
    'e': ['3', '4', 'w', 'r', 's', 'd'],
    'r': ['4', '5', 'e', 't', 'd', 'f'],
    't': ['5', '6', 'r', 'y', 'f', 'g'],
    # ...
    # 중단 행
    'a': ['q', 'w', 's', 'z'],
    's': ['q', 'w', 'e', 'a', 'd', 'z', 'x'],
    'd': ['w', 'e', 'r', 's', 'f', 'x', 'c'],
    # ...
    # 하단 행
    'z': ['a', 's', 'x'],
    'x': ['s', 'd', 'z', 'c'],
    # ...
    # 특수문자는 Shift 조합의 기본 키를 기준으로 매핑
    # 예: '!' → '1'의 인접 키 → Shift + 인접 키
}
```

**Shift 조합 매핑:**
```python
SHIFT_MAP = {
    '!': '1', '@': '2', '#': '3', '$': '4', '%': '5',
    '^': '6', '&': '7', '*': '8', '(': '9', ')': '0',
    '_': '-', '+': '=', '{': '[', '}': ']', '|': '\\',
    ':': ';', '"': "'", '<': ',', '>': '.', '?': '/',
    '~': '`',
}
# 대문자의 인접 키 = 소문자 인접 키의 대문자 변환
```

#### 3.2.4 오타 발생 → 수정 시퀀스

오타가 발생하고 수정이 결정되었을 때의 동작 흐름:

```
1. 오타 글자 입력 (예: 'r' 대신 't')
2. 인지 딜레이 (100~300ms, 가우시안)  ← 오타를 알아챈 시간
3. Backspace 입력 (빠른 연타: 각 30~50ms 간격)  ← burst backspace
4. Retype 준비 딜레이 (50~150ms)  ← 올바른 키로 손가락 재정렬
5. 올바른 글자 입력 ('r')
```

> **Backspace burst**: 사람이 오타를 수정할 때 backspace는 빠르게 연타하는 경향이 있다. 일반 타이핑 딜레이보다 짧은 간격(30~50ms)으로 입력한다.
>
> **Retype 준비 딜레이**: backspace 직후 올바른 글자를 입력하기까지, 손가락을 올바른 키 위치로 옮기는 재정렬 시간(50~150ms)이 발생한다. 이는 일반 타이핑의 base_delay와 별개의 독립된 딜레이이다.

**수정하지 않을 경우:** 오타 글자가 그대로 남고 다음 글자로 진행.

**전치(Transposition) 수정 시:**
```
1. 두 글자가 뒤바뀐 채 입력 (예: 't', 'e' 대신 'e', 't')
2. 인지 딜레이 (150~400ms)
3. Backspace × 2 (burst: 각 30~50ms 간격)
4. Retype 준비 딜레이 (50~150ms)
5. 올바른 순서로 재입력 ('t', 'e')
```

**이중 입력(Double Strike) 수정 시:**
```
1. 같은 글자 두 번 입력 (예: 's', 's')
2. 인지 딜레이 (80~200ms)
3. Backspace × 1
4. Retype 준비 딜레이 (30~80ms)  ← 같은 위치이므로 짧음
5. 다음 글자로 진행
```

#### 3.2.5 오타 판정 플로우

```python
def process_char(self, char, prev_char, next_char) -> list[Action]:
    actions = []

    # 오타 발생 여부 판정
    if random.random() < self.actual_typo_prob:
        # 활성화된 오타 유형 중 랜덤 선택
        typo_type = random.choice(self.enabled_typo_types)

        if typo_type == 'adjacent':
            wrong_char = self._get_adjacent_key(char)
            actions.append(Action('type', wrong_char))

            if random.random() < self.actual_revision_prob:
                actions.append(Action('pause', random.gauss(200, 50)))  # 인지 딜레이
                actions.append(Action('backspace', 1))                  # burst backspace
                actions.append(Action('pause', random.gauss(100, 30)))  # retype 준비 딜레이
                actions.append(Action('type', char))  # 올바른 글자
            # else: 오타 그대로 유지

        elif typo_type == 'transposition':
            # next_char와 순서 교환
            actions.append(Action('type', next_char))
            actions.append(Action('type', char))

            if random.random() < self.actual_revision_prob:
                actions.append(Action('pause', random.gauss(275, 60)))
                actions.append(Action('backspace', 2))  # burst backspace
                actions.append(Action('pause', random.gauss(100, 30)))  # retype 준비 딜레이
                actions.append(Action('type', char))
                actions.append(Action('type', next_char))
            return actions  # next_char도 처리했으므로 skip 플래그 필요

        elif typo_type == 'double_strike':
            actions.append(Action('type', char))  # 정상 입력
            actions.append(Action('type', char))  # 이중 입력

            if random.random() < self.actual_revision_prob:
                actions.append(Action('pause', random.gauss(140, 40)))
                actions.append(Action('backspace', 1))

    else:
        # 정상 입력
        actions.append(Action('type', char))

    return actions
```

---

### 3.3 타이핑 엔진 (`typer_engine.py`)

타이밍 모델과 오타 모델을 조합하여 실제 키 입력을 수행하는 메인 엔진.

#### 3.3.1 상태 머신

```
[IDLE] → (시작) → [COUNTDOWN] → (카운트다운 완료) → [TYPING] → (완료) → [DONE]
                                                        ↕ (일시정지/재개)
                                                    [PAUSED]
                                                        
[TYPING] → (정지/긴급정지) → [IDLE]
[PAUSED] → (정지) → [IDLE]
```

| 상태 | 설명 |
|------|------|
| `IDLE` | 대기 중. 입력 가능한 상태 |
| `COUNTDOWN` | 시작 전 카운트다운 진행 중 (기본 3초) |
| `TYPING` | 타이핑 진행 중 |
| `PAUSED` | 일시정지. 현재 위치 기억 |
| `DONE` | 타이핑 완료. 통계 표시 가능 |

#### 3.3.2 스레딩 모델

```
메인 스레드: GUI 이벤트 루프 (CustomTkinter)
├── 타이핑 스레드: typer_engine의 타이핑 루프 (daemon thread)
└── 핫키 스레드: pynput의 키보드 리스너 (daemon thread)
```

- 타이핑 스레드에서 GUI 업데이트 시 `root.after(0, callback)`으로 메인 스레드에 위임.
- 정지/일시정지는 `threading.Event` 객체로 제어.

#### 3.3.3 키 입력 시뮬레이션

두 가지 입력 모드를 제공한다. GUI 설정에서 전환 가능.

**간편 모드 (Simple Mode)** — 기본값. 안정적.
```python
from pynput.keyboard import Controller, Key

keyboard = Controller()

def simulate_key_simple(char):
    """단일 문자의 OS 레벨 키 이벤트 발생. Shift 조합 자동 처리."""
    if char == '\n':
        keyboard.press(Key.enter)
        keyboard.release(Key.enter)
    elif char == '\t':
        keyboard.press(Key.tab)
        keyboard.release(Key.tab)
    elif char == ' ':
        keyboard.press(Key.space)
        keyboard.release(Key.space)
    else:
        keyboard.type(char)  # press + release 자동 처리. Shift 조합도 자동.
```

**정교 모드 (Precise Mode)** — Shift 키를 명시적으로 press/release. 더 사람다운 키 이벤트 시퀀스 생성.
```python
def simulate_key_precise(char):
    """Shift 조합을 명시적으로 분리하여 더 정교한 키 이벤트 생성."""
    if char == '\n':
        keyboard.press(Key.enter)
        keyboard.release(Key.enter)
    elif char == '\t':
        keyboard.press(Key.tab)
        keyboard.release(Key.tab)
    elif char == ' ':
        keyboard.press(Key.space)
        keyboard.release(Key.space)
    elif char in SHIFT_CHARS:
        base_key = get_base_key(char)  # 'A' → 'a', '!' → '1' 등
        keyboard.press(Key.shift)
        time.sleep(random.gauss(0.015, 0.005))  # Shift 누른 후 미세 딜레이
        keyboard.press(base_key)
        keyboard.release(base_key)
        time.sleep(random.gauss(0.010, 0.003))  # 키 뗀 후 미세 딜레이
        keyboard.release(Key.shift)
    else:
        keyboard.press(char)
        keyboard.release(char)
```

> **정교 모드의 장점:** 실제 사람이 Shift+키를 누를 때 발생하는 `shift_down → key_down → key_up → shift_up` 시퀀스와 각 단계 간 미세한 시간 차이를 재현한다. Shift penalty 옵션과 결합하면 더 자연스러운 결과를 얻을 수 있다.
>
> **정교 모드의 주의사항:** 일부 환경에서 pynput의 press/release 직접 제어가 특수문자에 대해 예외를 발생시킬 수 있다. 문제 발생 시 간편 모드로 자동 폴백.

```python
def simulate_key(char):
    """현재 설정된 모드에 따라 적절한 입력 방식 선택."""
    try:
        if self.precise_mode:
            simulate_key_precise(char)
        else:
            simulate_key_simple(char)
    except Exception:
        # 정교 모드 실패 시 간편 모드로 폴백
        simulate_key_simple(char)
```

**Backspace 시뮬레이션:**
```python
def simulate_backspace(count=1):
    for _ in range(count):
        keyboard.press(Key.backspace)
        keyboard.release(Key.backspace)
        time.sleep(random.gauss(0.040, 0.008))  # burst backspace 간격 (30~50ms)
```

#### 3.3.4 드라이 런 모드

실제 키 입력을 하지 않고, 로그와 타이밍 데이터만 생성하는 모드.

```python
def type_text(self, text, dry_run=False):
    for i, char in enumerate(text):
        delay = self.timing_model.calculate_delay(char, prev_char, i, len(text))
        actions = self.typo_model.process_char(char, prev_char, next_char)

        for action in actions:
            if action.type == 'type':
                if not dry_run:
                    simulate_key(action.char)
                self.log(f"'{action.char}' 입력 ({delay:.0f}ms)")
            elif action.type == 'backspace':
                if not dry_run:
                    simulate_backspace(action.count)
                self.log(f"Backspace ×{action.count}")
            elif action.type == 'pause':
                time.sleep(action.duration / 1000)

            self.timing_data.append(delay)  # 시각화용 데이터 수집

        if not dry_run:
            time.sleep(delay / 1000)
        
        self.emit_progress(i + 1, len(text))
```

#### 3.3.5 콜백 시스템

엔진에서 GUI로 상태를 전달하기 위한 콜백 인터페이스:

```python
class TyperCallbacks:
    on_progress: Callable[[int, int], None]         # (현재, 전체) 진행률
    on_log: Callable[[str], None]                    # 로그 메시지
    on_state_change: Callable[[str], None]           # 상태 변경 (IDLE, TYPING, ...)
    on_typo: Callable[[str, str], None]              # (원래 글자, 오타 글자)
    on_complete: Callable[[dict], None]              # 통계 데이터
    on_countdown: Callable[[int], None]              # 카운트다운 (3, 2, 1)
```

---

### 3.4 클립보드 (`clipboard.py`)

```python
import pyperclip

def get_clipboard_text() -> str:
    """현재 클립보드의 텍스트를 반환. 텍스트가 아니면 빈 문자열."""
    try:
        text = pyperclip.paste()
        return text if isinstance(text, str) else ""
    except:
        return ""
```

- GUI에서 "새로고침" 버튼 클릭 시 호출.
- 자동 갱신 옵션: 1초 간격으로 폴링하여 클립보드 변경 감지 (선택적).

---

### 3.5 포커스 모니터 (`focus_monitor.py`)

타이핑 중 활성 창이 변경되면 엉뚱한 곳에 입력되는 사고를 방지하는 안전장치.

#### 3.5.1 동작 방식

```python
import ctypes

def get_active_window_title() -> str:
    """현재 활성(포커스) 창의 제목을 반환. Windows 전용."""
    hwnd = ctypes.windll.user32.GetForegroundWindow()
    length = ctypes.windll.user32.GetWindowTextLengthW(hwnd)
    buf = ctypes.create_unicode_buffer(length + 1)
    ctypes.windll.user32.GetWindowTextW(hwnd, buf, length + 1)
    return buf.value
```

#### 3.5.2 타이핑 엔진 연동

```
1. COUNTDOWN 완료 시점에 활성 창 제목을 기록 (target_window_title)
2. 타이핑 루프에서 N글자마다 (기본 10글자) 활성 창 제목을 확인
3. 활성 창이 변경되었으면:
   a. 자동 일시정지 (PAUSED 상태로 전환)
   b. 로그: "[안전장치] 활성 창 변경 감지 — 자동 일시정지"
   c. GUI에 경고 표시: "포커스가 이동했습니다. 원래 창으로 돌아간 후 재개하세요."
4. 재개(resume) 시 다시 활성 창 제목을 갱신
```

| 파라미터 | 타입 | 범위 | 기본값 | 설명 |
|----------|------|------|--------|------|
| `focus_check_interval` | int | 1~50 | 10 | N글자마다 포커스 확인 |

> GUI 설정에서 포커스 감시 ON/OFF 토글 가능. 기본값 ON.

---

### 3.6 텍스트 전처리 (`text_preprocessor.py`)

브라우저 입력 칸에 입력할 때 발생할 수 있는 공백/개행/길이 문제를 사전에 처리.

#### 3.6.1 전처리 옵션 `[각각 토글 ON/OFF]`

| 옵션 | 기본 | 설명 |
|------|------|------|
| **연속 공백 정규화** | OFF | 2개 이상 연속된 공백을 1개로 변환 |
| **개행 처리 모드** | Enter 유지 | `Enter 유지` / `Space로 치환` / `제거` 중 선택 |
| **CRLF → LF 정규화** | ON | `\r\n`을 `\n`으로 통일 |
| **최대 길이 제한** | OFF | ON 시 N자까지만 입력 (초과분 절삭). 기본 상한: 10000자 |
| **앞뒤 공백 제거 (Trim)** | ON | 텍스트 앞뒤의 불필요한 공백/개행 제거 |

#### 3.6.2 적용 시점

텍스트를 "사용"으로 설정하는 시점(클립보드 → "이 텍스트 사용" 또는 직접 입력 → "이 텍스트 사용")에 전처리를 적용한다. 원본 텍스트는 변경하지 않고, 전처리된 결과를 타이핑 대상으로 설정한다.

```python
def preprocess(text: str, options: PreprocessOptions) -> str:
    if options.crlf_normalize:
        text = text.replace('\r\n', '\n')
    if options.trim:
        text = text.strip()
    if options.normalize_spaces:
        text = re.sub(r' {2,}', ' ', text)
    if options.newline_mode == 'space':
        text = text.replace('\n', ' ')
    elif options.newline_mode == 'remove':
        text = text.replace('\n', '')
    if options.max_length_enabled:
        text = text[:options.max_length]
    return text
```

---

## 4. GUI 설계 (`gui/`)

### 4.1 메인 윈도우 (`app.py`)

- **프레임워크:** CustomTkinter
- **테마:** 다크 모드 기본 (시스템 설정 따름, 수동 토글 가능)
- **윈도우 크기:** 약 750 × 850px (리사이즈 가능, 최소 크기 제한)
- **Always on Top:** 토글 버튼으로 전환 가능

**레이아웃 (상하 3분할):**
```
┌─────────────────────────────────────────────────┐
│  [메뉴바]  프리셋: [▼ 드롭다운]  [저장] [불러오기]  │
│           📌 Always on Top [토글]                 │
├─────────────────────────────────────────────────┤
│  [1] 입력 소스 영역                                │
│      탭: [클립보드] [직접 입력]                      │
├─────────────────────────────────────────────────┤
│  [2] 설정 영역                                    │
│      탭: [타이밍] [오타]                           │
├─────────────────────────────────────────────────┤
│  [3] 컨트롤 영역                                  │
│      트리거/시작/정지/로그/진행률                    │
└─────────────────────────────────────────────────┘
```

---

### 4.2 입력 소스 영역 (`input_panel.py`)

**탭 1: 클립보드**

```
┌─────────────────────────────────────────────┐
│  📋 현재 클립보드 내용           [🔄 새로고침]  │
│  ┌─────────────────────────────────────────┐ │
│  │ The quick brown fox jumps over the      │ │
│  │ lazy dog. This is a test text for...    │ │
│  │                                         │ │
│  └─────────────────────────────────────────┘ │
│  글자 수: 52  │  예상 소요: ~4.2초              │
│                                [▶ 이 텍스트 사용] │
└─────────────────────────────────────────────┘
```

| 요소 | 설명 |
|------|------|
| 미리보기 영역 | 읽기 전용 텍스트 박스. 클립보드 내용 표시 (최대 5000자 미리보기) |
| 새로고침 버튼 | 클립보드 내용 다시 읽기 |
| 글자 수 | 미리보기 텍스트의 문자 수 |
| 예상 소요 시간 | 현재 설정 기준 대략적 소요 시간 계산 (`글자수 × base_delay_ms / 1000`) |
| "이 텍스트 사용" 버튼 | 이 텍스트를 타이핑 대상으로 설정 |

**탭 2: 직접 입력**

```
┌─────────────────────────────────────────────┐
│  ✏️ 텍스트 직접 입력                          │
│  ┌─────────────────────────────────────────┐ │
│  │ (여기에 타이핑할 텍스트를 입력하세요)       │ │
│  │                                         │ │
│  │                                         │ │
│  │                                         │ │
│  └─────────────────────────────────────────┘ │
│  글자 수: 0   │  예상 소요: ~0.0초              │
│                                [▶ 이 텍스트 사용] │
└─────────────────────────────────────────────┘
```

| 요소 | 설명 |
|------|------|
| 입력 영역 | 멀티라인 편집 가능 텍스트 박스 |
| 글자 수 | 실시간 업데이트 |
| "이 텍스트 사용" 버튼 | 이 텍스트를 타이핑 대상으로 설정 |

---

### 4.3 설정 영역 (`settings_panel.py`)

**탭 1: 타이밍 설정**

```
┌───────────────────────────────────────────────────┐
│  ⏱️ 타이밍 설정                                    │
│                                                    │
│  Base Delay (ms)      [====●========] 70           │
│  Delay Variance (ms)  [===●=========] 30           │
│                                                    │
│  ☑ Word Boundary                                   │
│    ├ Intra-word Factor  [======●====] 0.80         │
│    └ Inter-word Pause   [====●======] 120 ms       │
│                                                    │
│  ☑ Punctuation Pause    [=====●=====] 200 ms       │
│  ☑ Newline Pause        [======●====] 400 ms       │
│  ☑ Shift Penalty        [==●========] 25 ms        │
│  ☐ Double Letter Speed  [=======●===] 0.60         │
│  ☐ Burst Typing                                    │
│    ├ Burst Length        [2] ~ [5]                  │
│    └ Burst Pause         [===●=======] 40 ms       │
│  ☐ Fatigue Factor       [=●=========] 0.05         │
│                                                    │
│  입력 모드: (●) 간편 모드  ( ) 정교 모드              │
└───────────────────────────────────────────────────┘
```

**위젯 상세:**

| 파라미터 | 위젯 타입 | 동작 |
|----------|-----------|------|
| `base_delay_ms` | 슬라이더 + 수치 표시 | 20~500, step 5 |
| `delay_variance_ms` | 슬라이더 + 수치 표시 | 0~200, step 5 |
| Word Boundary | 체크박스 (토글) | OFF 시 하위 옵션 비활성화(회색 처리) |
| `intra_word_speed_factor` | 슬라이더 | 0.30~1.00, step 0.05 |
| `inter_word_pause_ms` | 슬라이더 | 0~500, step 10 |
| Punctuation Pause | 체크박스 + 슬라이더 | |
| Newline Pause | 체크박스 + 슬라이더 | 0~2000, step 50 |
| Shift Penalty | 체크박스 + 슬라이더 | |
| Double Letter Speed | 체크박스 + 슬라이더 | |
| Burst Typing | 체크박스 + min/max 스핀박스 + 슬라이더 | |
| Fatigue Factor | 체크박스 + 슬라이더 | 0.00~0.50, step 0.01 |
| 입력 모드 | 라디오 버튼 | 간편 모드 (기본) / 정교 모드 |

> 체크박스 OFF 시 해당 옵션의 슬라이더/입력이 **비활성화(회색 처리)** 되어 시각적으로 구분.

**탭 2: 오타 설정**

```
┌───────────────────────────────────────────────────┐
│  🔤 오타 설정                                      │
│                                                    │
│  오타 확률 (0~9999)    [    30    ] → 0.30%        │
│  수정 확률 (0~99)      [    85    ] → 85%          │
│                                                    │
│  오타 유형:                                        │
│  ☑ 인접 키 오타 (Adjacent Key)                     │
│  ☐ 글자 전치 (Transposition)                       │
│  ☐ 이중 입력 (Double Strike)                       │
│                                                    │
│  ⓘ 최소 1개 이상의 오타 유형이 활성화되어야 합니다.    │
│     (모두 OFF 시 오타 확률이 있어도 오타가 발생하지    │
│      않습니다.)                                     │
└───────────────────────────────────────────────────┘
```

**위젯 상세:**

| 파라미터 | 위젯 타입 | 동작 |
|----------|-----------|------|
| `typo_prob` | 숫자 입력 (Entry) | 0~9999 정수. 우측에 실제 % 환산 표시 |
| `typo_revision_prob` | 숫자 입력 (Entry) | 0~99 정수. 우측에 실제 % 환산 표시 |
| Adjacent Key | 체크박스 | |
| Transposition | 체크박스 | |
| Double Strike | 체크박스 | |

---

### 4.4 컨트롤 영역 (`control_panel.py`)

```
┌─────────────────────────────────────────────────────┐
│  🎮 컨트롤                                           │
│                                                      │
│  트리거 키: [ F6  ▼]                                  │
│  긴급 정지: ESC (Hard Stop)  │  트리거 키: Soft Stop  │
│  시작 전 카운트다운: [ 3 ] 초                           │
│  ☑ 포커스 감시 (이탈 시 자동 일시정지)                   │
│                                                      │
│  [▶ 시작]  [⏸ 일시정지]  [⏹ 정지]  [🧪 드라이 런]     │
│                                                      │
│  상태: ● 대기중                                       │
│  대상 텍스트: "The quick brown fox..." (52자)          │
│  진행: ░░░░░░░░░░░░░░░░░░░░ 0% (0/52)               │
│                                                      │
│  📜 실시간 로그                                       │
│  ┌─────────────────────────────────────────────────┐ │
│  │                                                 │ │
│  │  (타이핑 시작 시 로그가 여기에 표시됩니다)          │ │
│  │                                                 │ │
│  └─────────────────────────────────────────────────┘ │
│                                          [로그 지우기] │
└─────────────────────────────────────────────────────┘
```

**요소 상세:**

| 요소 | 위젯 | 설명 |
|------|------|------|
| 트리거 키 | 드롭다운 (F1~F12) | F1~F12 중 선택 (v1.0) |
| 긴급 정지 키 | 라벨 (고정) | ESC = Hard Stop. 변경 불가 |
| 카운트다운 | 스핀박스 | 0~10초. 0이면 카운트다운 없이 즉시 시작 |
| 포커스 감시 | 체크박스 | ON: 활성 창 변경 시 자동 일시정지. 기본 ON |
| 시작 버튼 | 버튼 | IDLE 상태에서만 활성화. 클릭 → COUNTDOWN → TYPING |
| 일시정지 버튼 | 버튼 | TYPING 상태에서만 활성화. Soft Stop과 동일 |
| 정지 버튼 | 버튼 | TYPING/PAUSED 상태에서 활성화. Hard Stop (IDLE 복귀) |
| 드라이 런 버튼 | 버튼 | IDLE 상태에서만 활성화. 실제 키 입력 없이 시뮬레이션 실행 |
| 상태 표시 | 라벨 | 현재 상태를 색상 원(●)과 텍스트로 표시 |
| 대상 텍스트 | 라벨 | 현재 설정된 타이핑 대상 텍스트 미리보기 (앞 40자 + "...") |
| 진행률 바 | 프로그레스 바 + 텍스트 | 퍼센트 + (현재/전체) 표시 |
| 실시간 로그 | 읽기 전용 텍스트 박스 | 자동 스크롤. 각 이벤트를 한 줄씩 표시 |
| 로그 지우기 | 버튼 | 로그 초기화 |

**상태별 색상:**

| 상태 | 원(●) 색상 | 텍스트 |
|------|------------|--------|
| IDLE | 회색 | 대기중 |
| COUNTDOWN | 노란색 | 카운트다운 (3... 2... 1...) |
| TYPING | 초록색 | 타이핑 중 |
| PAUSED | 주황색 | 일시정지 |
| DONE | 파란색 | 완료 |

**로그 출력 형식:**

```
[00:01.234] 'T' 입력 (72ms) [base:72]
[00:01.306] 'h' 입력 (58ms) [base:65 × intra_word:×0.80]
[00:01.364] 'r' 오타! (원래: 'e') (61ms)
[00:01.571] Backspace (207ms 후 인지)
[00:01.671] 'e' 수정 입력 (100ms retype 준비)
[00:01.698] ' ' 입력 (75ms) [base:75]
[00:01.841] 'q' 입력 (143ms) [base:68 + inter_word:+120 × fatigue:×1.02]
[00:02.100] 'H' 입력 (98ms) [base:73 + shift:+25]
...
[00:02.500] '\n' 입력 (85ms) [base:85]
[00:02.920] 'N' 입력 (520ms) [base:70 + newline:+400 + shift:+25]
...
[00:04.217] 완료. 통계를 확인하려면 [📊 통계] 버튼을 클릭하세요.
```

> 각 로그 줄의 `[...]` 태그는 딜레이 breakdown 기록에서 자동 생성. 어떤 규칙이 어떤 영향을 주었는지 한눈에 확인 가능.

---

### 4.5 통계 요약 + 타이밍 시각화 (`stats_dialog.py`)

타이핑 완료(또는 드라이 런 완료) 후 표시되는 다이얼로그 윈도우.

#### 4.5.1 통계 요약

```
┌─────────────────────────────────────────┐
│  📊 타이핑 통계                          │
│                                          │
│  총 소요 시간:    4.217초                 │
│  총 글자 수:      52자                    │
│  평균 속도:       216 CPM (43 WPM)       │
│                                          │
│  오타 발생:       2회 (3.8%)              │
│    ├ 인접 키:     1회                     │
│    ├ 전치:        1회                     │
│    └ 이중 입력:   0회                     │
│  오타 수정:       2회 (100%)              │
│  오타 미수정:     0회                     │
│                                          │
│  평균 딜레이:     72.3ms                  │
│  최소 딜레이:     31ms                    │
│  최대 딜레이:     287ms                   │
│  딜레이 표준편차: 38.2ms                  │
│                                          │
│  [📈 타이밍 시각화]        [닫기]          │
└─────────────────────────────────────────┘
```

#### 4.5.2 타이밍 시각화

"타이밍 시각화" 버튼 클릭 시, matplotlib 차트를 별도 윈도우 또는 다이얼로그 내 임베드로 표시.

**차트 1: 딜레이 분포 히스토그램**
- X축: 딜레이 (ms)
- Y축: 빈도
- 가우시안 분포에 가까운 형태가 자연스러움을 시각적으로 확인

**차트 2: 딜레이 타임라인 (산점도)**
- X축: 글자 인덱스 (0 ~ N)
- Y축: 딜레이 (ms)
- 각 점 = 하나의 글자 입력 딜레이
- 단어 경계, 구두점, 오타 발생 지점을 색상/마커로 구분
  - 파란 점: 일반 입력
  - 초록 점: 단어 내부 (빠름)
  - 빨간 점: 오타 발생
  - 노란 삼각형: 단어 경계 (느림)
  - 주황 사각형: 구두점 pause

> matplotlib이 설치되지 않은 경우, 시각화 버튼을 비활성화하고 "matplotlib 설치 필요" 표시.

---

## 5. 프리셋 시스템

### 5.1 프리셋 구조

JSON 파일로 모든 설정값을 저장/불러오기.

```json
{
    "preset_name": "느리고 자연스러운 타이핑",
    "preset_description": "천천히 정확하게 입력. 약간의 오타와 자연스러운 리듬.",

    "timing": {
        "base_delay_ms": 90,
        "delay_variance_ms": 40,
        "word_boundary_enabled": true,
        "intra_word_speed_factor": 0.75,
        "inter_word_pause_ms": 150,
        "punctuation_pause_enabled": true,
        "punctuation_pause_ms": 250,
        "newline_pause_enabled": true,
        "newline_pause_ms": 400,
        "shift_penalty_enabled": true,
        "shift_penalty_ms": 30,
        "double_letter_enabled": true,
        "double_letter_speed_factor": 0.55,
        "burst_enabled": true,
        "burst_length_min": 2,
        "burst_length_max": 4,
        "burst_pause_ms": 50,
        "fatigue_enabled": true,
        "fatigue_factor": 0.08,
        "precise_mode": false
    },

    "typo": {
        "typo_prob": 50,
        "typo_revision_prob": 90,
        "adjacent_key_enabled": true,
        "transposition_enabled": true,
        "double_strike_enabled": false
    },

    "control": {
        "trigger_key": "f6",
        "countdown_seconds": 3,
        "focus_monitor_enabled": true
    },

    "preprocessing": {
        "normalize_spaces": false,
        "newline_mode": "enter",
        "crlf_normalize": true,
        "max_length_enabled": false,
        "max_length": 10000,
        "trim": true
    }
}
```

### 5.2 기본 프리셋

| 프리셋 이름 | 속도 | 오타율 | 특징 |
|-------------|------|--------|------|
| **기본 (Default)** | 70ms | 0.30% | 모든 옵션 중간값. 균형 잡힌 설정 |
| **빠르고 정확한 (Fast & Accurate)** | 40ms | 0.10% | 숙련된 타이피스트. burst ON, 오타 적음 |
| **느리고 자연스러운 (Slow & Natural)** | 90ms | 0.50% | 천천히 정확하게. 모든 타이밍 옵션 ON |
| **오타 많은 초보 (Sloppy Beginner)** | 120ms | 3.00% | 느리고 오타 많음. Fatigue 강하게 |
| **로봇 (기준선)** | 50ms, variance 0 | 0% | 모든 자연스러움 옵션 OFF. 균일한 속도. 비교용 |

### 5.3 커스텀 프리셋

- GUI에서 설정을 조정한 뒤 "프리셋 저장" 버튼 클릭
- 이름 입력 다이얼로그 → `presets/` 폴더에 JSON 저장
- 프리셋 드롭다운에 자동 추가
- 삭제: 드롭다운에서 선택 후 "삭제" 버튼 (기본 프리셋은 삭제 불가)

---

## 6. 설정 저장/불러오기

### 6.1 자동 저장 (`config.json`)

앱 종료 시 현재 상태를 자동 저장하고, 다음 실행 시 복원.

**저장 항목:**
- 현재 모든 설정값 (타이밍 + 오타 + 컨트롤)
- 마지막 선택한 프리셋 이름
- 윈도우 위치/크기
- Always on Top 상태
- 활성 탭 (클립보드/직접 입력, 타이밍/오타)
- 직접 입력 탭의 텍스트 (선택적, 길이 제한 10000자)

### 6.2 저장 타이밍

- 앱 종료 시 (`on_closing` 이벤트)
- 설정 변경 시 자동 저장 (디바운스 2초 — 슬라이더를 움직이는 동안은 저장하지 않고, 멈춘 후 2초 뒤 저장)

---

## 7. 핫키 시스템

### 7.1 트리거 키

| 항목 | 설명 |
|------|------|
| 기본 키 | F6 |
| 변경 방법 | 드롭다운에서 F1~F12 중 선택 |
| 허용 키 (v1.0) | **F1~F12 단일키만** |
| 금지 키 | ESC(긴급 정지 전용) |
| 동작 | IDLE 상태: 타이핑 시작 (→ COUNTDOWN) / TYPING 상태: 일시정지 / PAUSED 상태: 재개 |

> **v1.1 예정:** Ctrl+Shift+조합, Alt+조합 등 복합 핫키 지원. pynput에서 조합키 핫키는 플랫폼/권한에 따라 동작이 불안정할 수 있어, v1.0에서는 안정적인 F1~F12 단일키만 지원한다.

### 7.2 정지 시스템 (2단계)

| 종류 | 키 | 동작 | 설명 |
|------|-----|------|------|
| **Soft Stop** | 트리거 키 (TYPING 상태에서) | 현재 글자 입력까지 완료 후 일시정지 (PAUSED) | 안전한 중단. 진행 중인 글자/오타 수정 시퀀스를 끝까지 마친 뒤 멈춤 |
| **Hard Stop (긴급 정지)** | ESC (고정, 변경 불가) | 즉시 중단, IDLE로 복귀 | 어떤 상태에서든 즉시 중단. 진행 중인 입력이 잘릴 수 있음 |

> **ESC의 우선순위:** 트리거 키보다 높음. TYPING/PAUSED/COUNTDOWN 어떤 상태에서든 작동.

### 7.3 핫키 리스너 구현

```python
from pynput import keyboard

class HotkeyListener:
    def __init__(self, trigger_key, on_trigger, on_hard_stop):
        self.trigger_key = trigger_key
        self.on_trigger = on_trigger       # 트리거: 시작/soft stop(일시정지)/재개
        self.on_hard_stop = on_hard_stop   # ESC: 즉시 중단
        self.listener = keyboard.Listener(on_press=self._on_press)
        self.listener.daemon = True

    def _on_press(self, key):
        if key == keyboard.Key.esc:
            self.on_hard_stop()
        elif key == self.trigger_key:
            self.on_trigger()

    def start(self):
        self.listener.start()

    def update_trigger(self, new_key):
        self.trigger_key = new_key
```

---

## 8. 일시정지/재개

### 8.1 동작 방식

```python
class TyperEngine:
    def __init__(self):
        self._pause_event = threading.Event()
        self._pause_event.set()  # 초기: 일시정지 아님
        self._stop_event = threading.Event()

    def pause(self):
        self._pause_event.clear()
        self.state = 'PAUSED'

    def resume(self):
        self._pause_event.set()
        self.state = 'TYPING'

    def stop(self):
        self._stop_event.set()
        self._pause_event.set()  # pause 대기 중이면 풀어줌
        self.state = 'IDLE'

    def _typing_loop(self, text):
        for i, char in enumerate(text[self._resume_index:], self._resume_index):
            # 일시정지 확인
            self._pause_event.wait()

            # 정지 확인
            if self._stop_event.is_set():
                return

            self._resume_index = i  # 현재 위치 기억
            # ... 타이핑 로직 ...
```

### 8.2 일시정지 시 GUI 표시

- 진행률 바 유지 (현재 위치)
- 상태: "일시정지 (32/52)" 표시
- 재개 시 현재 위치부터 이어서 입력

---

## 9. 드라이 런

### 9.1 동작

| 항목 | 설명 |
|------|------|
| 진입 | "드라이 런" 버튼 클릭 |
| 실제 키 입력 | **안 함** |
| 딜레이 대기 | **안 함** (즉시 완료) |
| 로그 출력 | **함** (각 글자의 계산된 딜레이, 오타 발생/수정 등 모두 기록) |
| 타이밍 데이터 수집 | **함** (시각화 가능) |
| 통계 표시 | **함** (완료 후 통계 다이얼로그) |

### 9.2 용도

- 설정을 바꿔가며 "이 설정이면 얼마나 자연스러운지" 로그와 타이밍 차트로 미리 확인
- 실제 브라우저에 입력하기 전에 오타 빈도, 딜레이 분포 등을 검증
- 로봇 프리셋 vs 자연스러운 프리셋의 차이를 시각적으로 비교

---

## 10. 에러 처리 및 예외 상황

### 10.1 예외 처리

| 상황 | 처리 |
|------|------|
| 클립보드에 텍스트 없음 | "클립보드가 비어있거나 텍스트가 아닙니다" 경고 표시 |
| 타이핑 대상 텍스트 미설정 | 시작 버튼 비활성화 + 안내 메시지 |
| 지원하지 않는 문자 포함 | 해당 문자 건너뛰고 로그에 "[스킵] U+XXXX 지원하지 않는 문자" 기록 |
| pynput 권한 오류 | 관리자 권한 실행 안내 다이얼로그 |
| config.json 손상 | 기본값으로 초기화 후 안내 |
| 프리셋 JSON 파싱 오류 | 해당 프리셋 무시 + 오류 로그 |
| 타이핑 중 포커스 이동 | 포커스 모니터 ON 시: 자동 일시정지 + 경고 로그. OFF 시: 사용자 책임 |

### 10.2 입력 검증

모든 숫자 입력 필드에 대해:
- 범위 초과 시 가장 가까운 유효값으로 클램핑
- 비숫자 문자 입력 시 무시
- 빈 칸은 기본값으로 복원

---

## 11. 개발 순서 (로드맵)

### Phase 1: 코어 엔진 (MVP)
1. `keyboard_map.py` — QWERTY 인접 키 딕셔너리 작성
2. `timing_model.py` — 기본 딜레이 + 가우시안 편차 + 단어 경계 + 구두점 + 개행 pause
3. `typo_model.py` — 인접 키 오타 + 수정 로직 (burst backspace + retype 준비 딜레이)
4. `typer_engine.py` — 통합 엔진 + 상태 머신 + 간편/정교 모드
5. `text_preprocessor.py` — 텍스트 전처리
6. CLI 테스트: 터미널에서 엔진 직접 실행하여 동작 확인

### Phase 2: GUI 기본
7. `app.py` — 메인 윈도우 프레임
8. `input_panel.py` — 클립보드/직접 입력 탭
9. `control_panel.py` — 시작/정지(Soft/Hard)/로그
10. 엔진 ↔ GUI 연결 (콜백, 스레딩)

### Phase 3: 설정 + 프리셋
11. `settings_panel.py` — 타이밍/오타/전처리 설정 패널
12. 설정 ↔ 엔진 바인딩
13. `config.json` 자동 저장/불러오기
14. 프리셋 시스템 (기본 프리셋 + 커스텀 저장/불러오기)

### Phase 4: 고급 기능
15. 고급 타이밍 옵션 (burst, fatigue, double letter, shift penalty, newline pause)
16. 일시정지/재개
17. 드라이 런
18. `focus_monitor.py` — 포커스 이탈 감지 + 자동 일시정지
19. Always on Top

### Phase 5: 통계 + 시각화
20. `stats_dialog.py` — 통계 요약
21. 타이밍 시각화 (matplotlib) + breakdown 기반 색상 구분
22. 드라이 런 ↔ 시각화 연동

### Phase 6: 마무리
23. 에러 처리 강화
24. 입력 검증
25. 테스트
26. README 작성 (사용 주의사항/면책 문구 포함)

### v1.1 예정 항목
- Ctrl+Shift+조합, Alt+조합 등 복합 핫키 트리거
- 한글 지원 (조합 문자 처리)
- 오타 수정 확률 동적 가중 (단어 길이, 문장 위치에 따라 revision_prob 변동)

---

## 12. 요구사항 요약 (`requirements.txt`)

```
customtkinter>=5.2.0
pynput>=1.7.6
pyperclip>=1.8.2
matplotlib>=3.7.0
```

---

## 부록 A: QWERTY 인접 키 맵 (전체)

```
`  → [1]
1  → [`, 2, q]
2  → [1, 3, q, w]
3  → [2, 4, w, e]
4  → [3, 5, e, r]
5  → [4, 6, r, t]
6  → [5, 7, t, y]
7  → [6, 8, y, u]
8  → [7, 9, u, i]
9  → [8, 0, i, o]
0  → [9, -, o, p]
-  → [0, =, p, [, ]]
=  → [-, ], \]

q  → [1, 2, w, a]
w  → [2, 3, q, e, a, s]
e  → [3, 4, w, r, s, d]
r  → [4, 5, e, t, d, f]
t  → [5, 6, r, y, f, g]
y  → [6, 7, t, u, g, h]
u  → [7, 8, y, i, h, j]
i  → [8, 9, u, o, j, k]
o  → [9, 0, i, p, k, l]
p  → [0, -, o, [, l, ;]
[  → [-, =, p, ], ;, ']
]  → [=, [, \, ']
\  → [=, ]]

a  → [q, w, s, z]
s  → [q, w, e, a, d, z, x]
d  → [w, e, r, s, f, x, c]
f  → [e, r, t, d, g, c, v]
g  → [r, t, y, f, h, v, b]
h  → [t, y, u, g, j, b, n]
j  → [y, u, i, h, k, n, m]
k  → [u, i, o, j, l, m, ,]
l  → [i, o, p, k, ;, ,, .]
;  → [o, p, [, l, ', .]
'  → [p, [, ], ;]

z  → [a, s, x]
x  → [a, s, d, z, c]
c  → [s, d, f, x, v]
v  → [d, f, g, c, b]
b  → [f, g, h, v, n]
n  → [g, h, j, b, m]
m  → [h, j, k, n, ,]
,  → [j, k, l, m, .]
.  → [k, l, ;, ,, /]
/  → [l, ;, .]
```

---

## 부록 B: Shift 조합 매핑

```
~  ↔  `        !  ↔  1        @  ↔  2        #  ↔  3
$  ↔  4        %  ↔  5        ^  ↔  6        &  ↔  7
*  ↔  8        (  ↔  9        )  ↔  0        _  ↔  -
+  ↔  =        {  ↔  [        }  ↔  ]        |  ↔  \
:  ↔  ;        "  ↔  '        <  ↔  ,        >  ↔  .
?  ↔  /
A-Z  ↔  a-z
```
